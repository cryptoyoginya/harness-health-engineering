# Node
node_modules/
*.log
.DS_Store

# Viewer build output
tools/viewer/dist/
tools/viewer/.vite/

# Semantic index (генерируется локально, переиндексируется по mtime)
.semantic-index.sqlite
.semantic-index.sqlite-wal
.semantic-index.sqlite-shm

# SkillOpt metrics
.skillopt-metrics.sqlite
.skillopt-metrics.sqlite-wal
.skillopt-metrics.sqlite-shm
.skillopt.local.json

# Секреты (Whoop client id/secret и пр.)
.env

# Whoop OAuth-токены (refresh/access) — НИКОГДА не коммитим
.whoop/

# Telegram-бот: служебный offset long-polling
.bot/

# Визуальный дневник: селфи лица — биометрия, НИКОГДА не коммитим (репо публичный).
# В git попадает только ссылка-имя файла в daily-логе, не само изображение.
01_raw/health/photos/

# Наблюдения по внешности/телу — чувствительное, держим локально (репо публичный).
04_synthesis/visual-diary.md
04_synthesis/stool-diary.md
04_synthesis/food-diary.md

# Личные записи-дневники (свободный текст) — приватно, репо публичный.
# Whoop-логи о здоровье (цифры) остаются публичными; прячем только личный free-text.
00_context/north-star.md
01_raw/health/2026-06-13.md

# Кэш моделей transformers.js (~120 MB)
scripts/semantic/.transformers-cache/

# Рабочая зона: черновики, attachments, dream-reports
.context/

# Сессионная working memory (core.md коммитим, остальное нет)
.remember/now.md
.remember/today.md

# Служебные локи Claude
.claude/scheduled_tasks.lock

# IDE
.vscode/
.idea/
*.swp
