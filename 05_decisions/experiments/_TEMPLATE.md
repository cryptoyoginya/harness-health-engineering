---
type: experiment
status: template
related:
  - /00_context/health-metrics.md
---

# EXP-NNN: <короткое название>

> n-of-1 trial (single-subject design). Дизайн фиксируется ДО данных — это и есть научность.
> Подробно о методе: [`/METHODOLOGY.md`](../../METHODOLOGY.md).

DECISION: <что меняю — одной фразой>. [source: /04_synthesis/<...>.md или /01_raw/health/<...>.md]

- **Гипотеза:** <причинно-следственная связь — конкретно>
- **Что меняю:** <ровно ОДНА переменная>. Остальное держу неизменным (иначе эффект не атрибутировать).
  Только один активный эксперимент за раз — параллельные интервенции конфаундят друг друга.
- **Baseline (до):** <медиана за стабилизационную неделю, не один день>.
  ⚠️ Не стартовать «со дна»: если baseline взят из ямы, любое улучшение может быть **регрессией к
  среднему**, а не эффектом. База должна быть стабильной (низкий разброс), не худшим днём.
- **Срок:** <N недель> (до DD.MM)
- **Первичная метрика (ровно одна):** <одна метрика-исход, выбранная ДО данных>.
  Вторичные метрики можно наблюдать, но вердикт выносится **только по первичной** — критерий по
  нескольким метриками через «ИЛИ» раздувает ложные срабатывания (множественные сравнения).
- **Критерий успеха (пред-регистрация):** <порог по первичной метрике, записанный ДО данных, и
  **выше обычного недельного разброса** baseline — напр. recovery avg +10 п.п., если неделя-к-неделе гуляет на ±4>
- **Washout / реверс (по умолчанию ДА):** вернуть переменную в конце и проверить, что эффект уходит.
  Без реверса вывод остаётся корреляционным — confidence ниже.

## Итог (заполнить по окончании) — рубрика вердикта

`merge` только если выполнены ВСЕ пункты, иначе `revert` или `продлить`:

- [ ] **Первичная метрика** прошла пред-зарегистрированный порог (а не вторичная, подобранная постфактум).
- [ ] Эффект **больше обычного недельного разброса** baseline (не шум).
- [ ] **Регрессия к среднему** исключена (baseline был стабильным, не «дном»).
- [ ] **Washout/реверс** подтвердил уход эффекта (или честно отмечено, что не проверяли → confidence ниже).
- [ ] **Confounders** названы (срывы, болезнь, события, цикл, сезон) — неконтролируемые → UNKNOWN.

- **Результат:** <цифры после vs baseline vs критерий>
- **Confounders:** <что могло исказить — отметить как UNKNOWN, если не контролировалось>
- DECISION: merge (→ правило в `CLAUDE.md`) / revert (откат, записать почему) / продлить.
- Перенести вывод в `/03_wiki/health.md` § «что пробовал».
